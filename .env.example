# Gemini — web research (Google Search grounding), deep narrative, and optional research model override
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-pro
GEMINI_RESEARCH_MODEL=gemini-2.5-flash

# OpenAI — resume profile parse, resume market value, core narrative
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1

# Gemini — deep explanatory fields (personality, failure mode, fallback plan)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-pro
